sudo: required

services:

  - docker


before_install:
  - docker build -t imranafzal/react-test -f ./client/Dockerfile.dev ./
script:

  - docker run imranafzal/react-test npm test -- --coverage


after_success:

  - docker build -t imranafzal/multi-client ./client

  - docker build -t imranafzal/multi-nginx ./nginx

  - docker build -t imranafzal/multi-server ./server

  - docker build -t imranafzal/multi-worker ./worker
  
# Log in to the docker CLI

  - echo "$DOCKER_PASSWORD" | docker login -u "$DOCKER_ID" --password-stdin
 
# Take those images and push them to Docke hub

  - docker push imranafzal/multi-client

  - docker push imranafzal/multi-nginx

  - docker push imranafzal/multi-server

  - docker push imranafzal/multi-worker

